# inbox/

Drop a friend's raw idea docs here (`.docx`, `.pdf`, `.md`, `.txt`), then run the distiller to turn them
into the spec pack (`mission/spec/`) and the Statement of Work (`mission/SOW.md`).

Everything in this folder except this README and `.gitkeep` is **git-ignored** — friends' raw documents
never enter repo history.
